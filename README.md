A simple [Pandoc](https://pandoc.org/) filter for generating mermaid diagrams within `mermaid` code blocks.
Images will be generated as PNG files or PDFs depending on the output format of the document.
